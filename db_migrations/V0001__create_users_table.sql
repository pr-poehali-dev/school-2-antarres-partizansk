CREATE TABLE IF NOT EXISTS users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL, role TEXT NOT NULL, class_name TEXT, status TEXT NOT NULL DEFAULT 'pending', created_at TIMESTAMP DEFAULT NOW());